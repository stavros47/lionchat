{
  "name": "lionchat",
  "version": "1.0.0",
  "description": "A basic Chat web application for learning purposes",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"

  },
  "chromium-args" : "--enable-logging=stderr",
  "author": "",
  "license": "ISC",

  "dependencies": {
    "socket.io": "*",
    "express": "*"
  }
}
